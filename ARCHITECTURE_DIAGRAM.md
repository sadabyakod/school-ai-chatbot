# Architecture Diagram - Before and After Migration

## 🔴 BEFORE Migration

```
┌─────────────────────────────────────────────────────────────────┐
│                         FRONTEND                                 │
│                 (React + Vite + TypeScript)                      │
│                    http://localhost:5173                         │
└────────────────────┬────────────────────────────────────────────┘
                     │
                     │ HTTP Requests
                     │
        ┌────────────┴─────────────┐
        │                          │
        ▼                          ▼
┌──────────────┐          ┌─────────────────┐
│   ASP.NET    │          │ Azure Functions │
│     Core     │          │  (SmartStudyFunc)│
│   Backend    │          │                 │
├──────────────┤          ├─────────────────┤
│ • FileUpload │          │ • SearchRagQuery│
│ • Simple Chat│          │ • GenStudyNotes │
│ • Auth       │          │ • ChatHistory   │
└──────┬───────┘          │ • EmbedService  │
       │                  └────────┬────────┘
       │                           │
       │  ┌────────────────────────┘
       │  │
       ▼  ▼
┌──────────────────────┐
│   Azure SQL Server   │
│                      │
└──────────────────────┘

Issues:
❌ Duplicate RAG logic
❌ In-memory chat (lost on restart)
❌ No conversation continuity
❌ Split architecture (harder to maintain)
```

---

## 🟢 AFTER Migration

```
┌─────────────────────────────────────────────────────────────────┐
│                         FRONTEND                                 │
│                 (React + Vite + TypeScript)                      │
│                    http://localhost:5173                         │
│                                                                   │
│  NEW FEATURES:                                                   │
│  • sessionId tracking for chat continuity                        │
│  • Study notes component (optional)                              │
└────────────────────┬────────────────────────────────────────────┘
                     │
                     │ HTTP Requests (with sessionId)
                     │
                     ▼
    ┌────────────────────────────────────────────┐
    │        ASP.NET Core Backend                │
    │   (Consolidated - All Features)            │
    │                                             │
    │  ┌──────────────────────────────────────┐  │
    │  │         CONTROLLERS                   │  │
    │  ├──────────────────────────────────────┤  │
    │  │ ChatController                        │  │
    │  │  • POST /chat (RAG + History)         │  │
    │  │  • GET /chat/history                  │  │
    │  │  • GET /chat/sessions                 │  │
    │  ├──────────────────────────────────────┤  │
    │  │ NotesController (NEW!)                │  │
    │  │  • POST /notes/generate               │  │
    │  │  • GET /notes                         │  │
    │  │  • GET /notes/{id}                    │  │
    │  │  • POST /notes/{id}/rate              │  │
    │  ├──────────────────────────────────────┤  │
    │  │ FileController                        │  │
    │  │  • POST /upload (ingestion only)      │  │
    │  └──────────────────────────────────────┘  │
    │                                             │
    │  ┌──────────────────────────────────────┐  │
    │  │         SERVICES (NEW!)               │  │
    │  ├──────────────────────────────────────┤  │
    │  │ RAGService                            │  │
    │  │  • GetEmbedding()                     │  │
    │  │  • SearchSimilarContent()             │  │
    │  │  • BuildContextText()                 │  │
    │  ├──────────────────────────────────────┤  │
    │  │ ChatHistoryService (NEW!)             │  │
    │  │  • SaveChatHistory()                  │  │
    │  │  • GetChatHistoryBySession()          │  │
    │  │  • GetUserChatSessions()              │  │
    │  ├──────────────────────────────────────┤  │
    │  │ StudyNotesService (NEW!)              │  │
    │  │  • GenerateStudyNotes()               │  │
    │  │  • GetUserStudyNotes()                │  │
    │  │  • RateStudyNote()                    │  │
    │  ├──────────────────────────────────────┤  │
    │  │ OpenAiChatService                     │  │
    │  │ JwtService                            │  │
    │  └──────────────────────────────────────┘  │
    │                                             │
    └─────────────────┬───────────────────────────┘
                      │
                      ▼
    ┌─────────────────────────────────────────┐
    │     Azure SQL Server (EF Core)          │
    │                                          │
    │  EXISTING TABLES:                        │
    │  • Users, Schools, Faqs                  │
    │  • SyllabusChunks, Embeddings            │
    │  • ChatLogs, UploadedFiles               │
    │                                          │
    │  NEW TABLES (Migrated):                  │
    │  • ChatHistories ✨                      │
    │  • StudyNotes ✨                         │
    │                                          │
    │  INDEXES:                                │
    │  • IX_ChatHistory_UserSession            │
    │  • IX_StudyNote_UserCreated              │
    └──────────────────────────────────────────┘

Benefits:
✅ Single codebase (easier maintenance)
✅ SQL-backed chat history (persistent)
✅ Session-based continuity
✅ Centralized RAG service (DRY)
✅ Study notes with ratings
✅ Better separation of concerns
✅ SQL-based vector search (no external dependencies)
```

---

## 🔄 Data Flow Examples

### Example 1: Chat with RAG (with history)

```
┌─────────┐  1. POST /chat          ┌──────────────┐
│Frontend │  { question, sessionId } │ ChatController│
│         ├─────────────────────────►│              │
└─────────┘                          └──────┬───────┘
                                            │
                            2. Search RAG   │
                                            ▼
                                     ┌──────────────┐
                                     │  RAGService  │
                                     │ • GetEmbed   │
                                     │ • SearchSim  │
                                     │ • BuildCtx   │
                                     └──────┬───────┘
                                            │
                    3. SQL Cosine Similarity│
                                            ▼
                                     ┌──────────────┐
                                     │ SQL Database │
                                     │ChunkEmbeddings│
                                     └──────┬───────┘
                                            │
                       4. Get top-K chunks  │
                                            ▼
                                     ┌──────────────┐
                                     │  FileChunks  │
                                     │ ChunkText    │
                                     └──────┬───────┘
                                            │
                        5. Generate answer  │
                                            ▼
                                     ┌──────────────┐
                                     │OpenAI Service│
                                     │  GPT-4       │
                                     └──────┬───────┘
                                            │
                        6. Save to history  │
                                            ▼
                                     ┌──────────────┐
                                     │ChatHistorySvc│
                                     │ SaveHistory()│
                                     └──────┬───────┘
                                            │
                                            ▼
┌─────────┐  7. Response              ┌──────────────┐
│Frontend │  { reply, sessionId }      │ SQL Database │
│         │◄───────────────────────────┤ChatHistories │
└─────────┘                            └──────────────┘
```

### Example 2: Generate Study Notes

```
┌─────────┐  1. POST /notes/generate  ┌──────────────┐
│Frontend │  { topic, subject }        │NotesController│
│         ├───────────────────────────►│              │
└─────────┘                            └──────┬───────┘
                                              │
                          2. Generate notes   │
                                              ▼
                                       ┌──────────────┐
                                       │StudyNotesSvc │
                                       └──────┬───────┘
                                              │
                           3. Search RAG      │
                                              ▼
                                       ┌──────────────┐
                                       │  RAGService  │
                                       │SearchSimilar │
                                       └──────┬───────┘
                                              │
                      4. Get relevant chunks  │
                                              ▼
                                       ┌──────────────┐
                                       │ SQL Database │
                                       │ChunkEmbeddings│
                                       └──────┬───────┘
                                              │
                        5. Generate notes     │
                                              ▼
                                       ┌──────────────┐
                                       │OpenAI Service│
                                       │ GPT-4        │
                                       └──────┬───────┘
                                              │
                         6. Save to DB        │
                                              ▼
                                       ┌──────────────┐
                                       │ SQL Database │
                                       │  StudyNotes  │
                                       └──────┬───────┘
                                              │
                                              ▼
┌─────────┐  7. Response               ┌──────────────┐
│Frontend │  { noteId, notes (md) }    │NotesController│
│         │◄───────────────────────────┤              │
└─────────┘                            └──────────────┘
```

---

## 🗂️ Service Dependencies

```
┌─────────────────────────────────────────────────────┐
│                    CONTROLLERS                       │
│  (Depend on Services via Dependency Injection)      │
└──────────────┬──────────────────────────────────────┘
               │
   ┌───────────┼───────────┐
   │           │           │
   ▼           ▼           ▼
┌─────┐  ┌──────────┐  ┌────────┐
│Chat │  │  Notes   │  │  File  │
│Ctrl │  │  Ctrl    │  │  Ctrl  │
└──┬──┘  └────┬─────┘  └───┬────┘
   │          │            │
   │ Uses:    │ Uses:      │ Uses:
   │ • RAG    │ • RAG      │ • Blob
   │ • Chat   │ • Notes    │ • Chat
   │   History│ • Chat     │   History
   │          │            │
   ▼          ▼            ▼
┌────────────────────────────────┐
│         SERVICES LAYER          │
├────────────────────────────────┤
│ • RAGService (shared by all)   │
│ • ChatHistoryService            │
│ • StudyNotesService             │
│ • OpenAiChatService             │
│ • BlobStorageService            │
│ • JwtService                    │
└────────────┬───────────────────┘
             │
             ▼
┌────────────────────────────────┐
│         DATA LAYER              │
├────────────────────────────────┤
│ • AppDbContext (EF Core)        │
│ • Azure SQL Server              │
│ • Azure Blob Storage            │
└────────────────────────────────┘
```

---

## 📊 Database ER Diagram (New Tables)

```
┌─────────────────────────┐
│      ChatHistories      │
├─────────────────────────┤
│ Id (PK)                 │
│ UserId (indexed)        │
│ SessionId (indexed)     │
│ Message                 │
│ Reply                   │
│ Timestamp (indexed)     │
│ ContextUsed (JSON)      │
│ ContextCount            │
│ AuthenticatedUserId (FK)│
└─────────┬───────────────┘
          │
          │ FK (optional)
          │
          ▼
┌─────────────────────────┐
│        Users            │
├─────────────────────────┤
│ Id (PK)                 │
│ Email                   │
│ PasswordHash            │
│ ...                     │
└─────────┬───────────────┘
          │
          │ FK (optional)
          │
          ▼
┌─────────────────────────┐
│      StudyNotes         │
├─────────────────────────┤
│ Id (PK)                 │
│ UserId (indexed)        │
│ Topic                   │
│ GeneratedNotes (md)     │
│ SourceChunks (JSON)     │
│ Subject                 │
│ Grade                   │
│ Chapter                 │
│ CreatedAt (indexed)     │
│ AuthenticatedUserId (FK)│
│ Rating (1-5)            │
└─────────────────────────┘

Relationships:
- ChatHistories → Users (many-to-one, optional)
- StudyNotes → Users (many-to-one, optional)
- Both support anonymous users (UserId = IP address)
```

---

## 🎯 Migration Path

```
Step 1: Code Migration
├─ Create Models (ChatHistory, StudyNote)
├─ Create Services (ChatHistory, RAG, StudyNotes)
├─ Update Controllers (Chat, new Notes)
└─ Update Program.cs (DI registration)
         │
         ▼
Step 2: Database Migration
├─ dotnet ef migrations add AddChatHistoryAndStudyNotes
├─ dotnet ef database update
└─ Verify tables created
         │
         ▼
Step 3: Local Testing
├─ dotnet build
├─ dotnet run
└─ Run test-endpoints.ps1
         │
         ▼
Step 4: Frontend Update
├─ Add sessionId tracking
├─ Update chat API calls
└─ (Optional) Add StudyNotes component
         │
         ▼
Step 5: Deploy to Azure
├─ git push origin main
├─ GitHub Actions deploys
└─ Verify production endpoints
         │
         ▼
Step 6: Retire Azure Functions
├─ Archive C:\SmartStudyFunc
├─ Stop Azure Function App
└─ (Optional) Delete resources
```

---

## 🚀 Deployment Architecture

```
┌─────────────────────────────────────────────┐
│           GitHub Repository                  │
│        (school-ai-chatbot)                  │
└────────────┬────────────────────────────────┘
             │
             │ git push
             │
             ▼
┌─────────────────────────────────────────────┐
│         GitHub Actions CI/CD                │
│                                              │
│ Frontend:                Backend:            │
│ • Build Vite            • Build .NET        │
│ • Deploy to SWA         • Deploy to App Svc │
└────┬─────────────────────────────┬──────────┘
     │                             │
     ▼                             ▼
┌──────────────┐          ┌─────────────────┐
│Azure Static  │          │ Azure App Svc   │
│  Web Apps    │  ◄─────► │  (Backend API)  │
│              │   HTTP   │                 │
│ Frontend     │          │ • Chat API      │
│ React+Vite   │          │ • Notes API     │
└──────────────┘          │ • File Upload   │
                          └────────┬────────┘
                                   │
                   ┌───────────────┼───────────────┐
                   │               │               │
                   ▼               ▼               ▼
            ┌───────────┐   ┌───────────┐  ┌───────────┐
            │Azure SQL  │   │Azure Blob │  │  OpenAI   │
            │ Database  │   │  Storage  │  │    API    │
            └───────────┘   └───────────┘  └───────────┘
```

This architecture is simpler, more maintainable, and more cost-effective with SQL-based vector search!
